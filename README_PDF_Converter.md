# HTML to PDF Converter - تحويل ملفات HTML إلى PDF تفاعلي

مجموعة أدوات Python لتحويل ملفات HTML التعليمية إلى ملفات PDF تفاعلية مع نظام carousel navigation.

## 🚀 الميزات الرئيسية

### ✨ المحول الأساسي (`html_to_pdf_converter.py`)
- تحويل كل card في HTML إلى صفحة منفصلة في PDF
- دعم النصوص العربية (RTL) والكود (LTR)
- إنشاء فهرس محتويات تلقائي
- نظام navigation بين الصفحات
- دعم تحويل ملفات متعددة

### 🎨 المحول المتقدم (`carousel_pdf_converter.py`)
- تصميم احترافي مع ألوان وخطوط محسنة
- روابط تفاعلية للتنقل بين الصفحات
- bookmarks تلقائية لكل card
- تحسين توزيع المحتوى على الصفحات
- معالجة ذكية للنصوص الطويلة

## 📦 المتطلبات

تثبيت المكتبات المطلوبة:

```bash
pip install -r requirements_pdf.txt
```

المكتبات المطلوبة:
- `reportlab` - إنشاء ملفات PDF
- `beautifulsoup4` - تحليل HTML
- `weasyprint` - تحويل HTML إلى PDF (اختياري)
- `lxml` - معالجة XML/HTML

## 🛠️ كيفية الاستخدام

### الاستخدام السريع

```python
# استيراد المحول
from html_to_pdf_converter import HTMLToPDFConverter

# إنشاء instance
converter = HTMLToPDFConverter()

# تحويل ملف واحد
success = converter.convert_to_pdf(
    "explain/4.1 reflection_pattern_explained.html",
    "output/reflection_pattern.pdf"
)

# تحويل ملفات متعددة
results = converter.convert_multiple_files(
    "explain",      # مجلد الإدخال
    "output/pdfs",  # مجلد الإخراج
    "*.html"        # نمط الملفات
)
```

### المحول المتقدم

```python
# استيراد المحول المتقدم
from carousel_pdf_converter import CarouselPDFConverter

# إنشاء instance
converter = CarouselPDFConverter()

# تحويل مع ميزات متقدمة
success = converter.convert_to_pdf(
    "explain/compliance_in_agentic_ai_explained.html",
    "output/compliance_carousel.pdf"
)

# تحويل دفعي
results = converter.batch_convert(
    "explain",           # مجلد الإدخال  
    "output/carousel"    # مجلد الإخراج
)
```

## 🧪 اختبار المحولات

تشغيل اختبارات شاملة:

```bash
python test_pdf_converter.py
```

الاختبارات تتضمن:
- ✅ تحليل بنية HTML
- ✅ تحويل ملف واحد
- ✅ تحويل دفعي
- ✅ قياس الأداء وحجم الملفات

## 📁 بنية المشروع

```
project/
├── html_to_pdf_converter.py     # المحول الأساسي
├── carousel_pdf_converter.py    # المحول المتقدم
├── test_pdf_converter.py        # اختبارات شاملة
├── requirements_pdf.txt         # المتطلبات
├── explain/                     # ملفات HTML الأصلية
│   ├── *.html                   # ملفات التعليمات
│   └── pdfs/                    # مجلد الـ PDF المُخرج
└── README_PDF_Converter.md      # هذا الملف
```

## 🎯 ميزات المحتوى

### معالجة البطاقات (Cards)
- **استخراج تلقائي**: يستخرج كل `<div class="card">` كصفحة منفصلة
- **العناوين**: يستخرج H2, H3 أو .card-title كعناوين
- **المحتوى**: يجمع كل الفقرات والقوائم
- **الكود**: يحدد ويعالج كتل الكود بشكل منفصل

### التنسيق والتخطيط
- **النصوص العربية**: محاذاة يمين (RTL) مع دعم Unicode
- **كتل الكود**: محاذاة يسار (LTR) مع خط Monospace
- **الألوان**: نظام ألوان متسق مع HTML الأصلي
- **التباعد**: توزيع مناسب للمحتوى

### نظام التنقل
- **فهرس المحتويات**: صفحة أولى تحتوي على روابط لكل البطاقات
- **أزرار التنقل**: السابق/التالي في أسفل كل صفحة
- **مؤشر الصفحة**: "صفحة X من Y"
- **العودة للفهرس**: رابط سريع في كل صفحة

## ⚙️ خيارات التخصيص

### إعدادات الصفحة

```python
converter = HTMLToPDFConverter(
    page_size=A4,              # حجم الصفحة
    font_size=12,              # حجم الخط الأساسي
    title_font_size=18,        # حجم خط العناوين
    margin=0.75 * inch         # الهوامش
)
```

### إعدادات الخطوط

```python
# تخصيص مسارات الخطوط العربية
font_paths = [
    '/path/to/arabic/font.ttf',
    '/another/path/font.ttf'
]
```

### إعدادات الألوان

```python
# في المحول المتقدم
converter.primary_color = Color(0.17, 0.24, 0.31)    # لون العناوين
converter.secondary_color = Color(0.90, 0.30, 0.24)  # لون التمييز
converter.nav_color = Color(0.0, 0.48, 1.0)          # لون التنقل
```

## 🔧 استكشاف الأخطاء وإصلاحها

### مشاكل شائعة وحلولها

#### 1. خطأ في تثبيت WeasyPrint

```bash
# macOS
brew install cairo pango gdk-pixbuf libffi

# Ubuntu/Debian
sudo apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info

# ثم تثبيت WeasyPrint
pip install weasyprint
```

#### 2. مشاكل الخطوط العربية

```python
# إضافة مسارات خطوط إضافية
font_paths = [
    '/System/Library/Fonts/Helvetica.ttc',  # macOS
    '/usr/share/fonts/TTF/',                 # Linux
    'C:/Windows/Fonts/',                     # Windows
]
```

#### 3. ملفات HTML غير موجودة

```python
# تحقق من وجود الملف
import os
if not os.path.exists("explain/file.html"):
    print("ملف HTML غير موجود")
```

#### 4. مشاكل الأذونات

```bash
# تأكد من أذونات الكتابة
chmod 755 output/
mkdir -p output/pdfs
```

## 📊 معلومات الأداء

### أحجام الملفات المتوقعة
- **ملف HTML متوسط** (8 cards): ~150-300 KB PDF
- **ملف HTML كبير** (15+ cards): ~400-600 KB PDF
- **ملفات مع كود كثير**: +50-100 KB إضافية

### سرعة التحويل
- **ملف واحد**: 2-5 ثواني
- **10 ملفات**: 20-50 ثانية
- **الدفعة الكاملة** (20+ ملف): 1-3 دقائق

## 🎨 أمثلة الاستخدام المتقدم

### تحويل مخصص مع إعدادات خاصة

```python
from html_to_pdf_converter import HTMLToPDFConverter
from reportlab.lib.pagesizes import letter

# إعدادات مخصصة
converter = HTMLToPDFConverter(
    page_size=letter,          # استخدام حجم letter بدل A4
    font_size=14,              # خط أكبر
    title_font_size=20,        # عناوين أكبر
    margin=1.0 * inch          # هوامش أوسع
)

# تحويل مع إعدادات خاصة
success = converter.convert_to_pdf(
    "input.html", 
    "output_custom.pdf"
)
```

### معالجة أخطاء شاملة

```python
import logging

# تفعيل التسجيل المفصل
logging.basicConfig(level=logging.DEBUG)

try:
    converter = HTMLToPDFConverter()
    results = converter.convert_multiple_files("explain", "output")
    
    # تحليل النتائج
    successful = [f for f, status in results.items() if status]
    failed = [f for f, status in results.items() if not status]
    
    print(f"✅ نجح: {len(successful)} ملف")
    print(f"❌ فشل: {len(failed)} ملف")
    
    if failed:
        print("الملفات الفاشلة:")
        for file in failed:
            print(f"  - {file}")
            
except Exception as e:
    print(f"خطأ عام: {e}")
```

## 🤝 المساهمة والتطوير

### إضافة ميزات جديدة

1. **ميزات مقترحة**:
   - دعم صور في البطاقات
   - إضافة watermarks
   - تصدير bookmarks تفاعلية
   - دعم themes متعددة

2. **تحسينات الأداء**:
   - معالجة متوازية للملفات
   - cache للخطوط والأنماط
   - ضغط أفضل للـ PDF

### هيكل الكود

```python
class HTMLToPDFConverter:
    def __init__(self):           # الإعداد الأولي
    def extract_cards(self):      # استخراج البطاقات
    def create_toc(self):         # إنشاء الفهرس
    def create_card_page(self):   # إنشاء صفحة البطاقة
    def convert_to_pdf(self):     # التحويل الرئيسي
```

## 📄 الترخيص والاستخدام

هذه الأدوات مفتوحة المصدر ومتاحة للاستخدام التعليمي والتجاري. 

---

## 🆘 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تأكد من تثبيت كل المتطلبات
2. شغل `test_pdf_converter.py` للتشخيص
3. راجع رسائل الخطأ في console
4. تأكد من أن ملفات HTML تحتوي على بطاقات بالتنسيق المتوقع

**تم إنشاء هذه الأدوات خصيصاً لمحتوى Agentic AI التعليمي** 🤖📚
