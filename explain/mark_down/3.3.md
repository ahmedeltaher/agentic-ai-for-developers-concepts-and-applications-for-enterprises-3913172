# إعداد الـ Indexes للـ Router - شرح بالعامية المصرية

<div dir="rtl">

## بيئة العمل: GitHub Codespaces

### إزاي نبدأ؟

**الطريقة الأولى:**
- اضغط على **اللينك تحت الفيديو**

**الطريقة الثانية:**
1. روح على **الـ Course Repo**
2. اضغط على **Code**
3. اختار **Codespaces**

> ⚠️ **ملحوظة:** أول مرة هتفتح Codespace ممكن **ياخد وقت شوية**

### فتح الـ Notebook
```
📁 Course Files
    ⬇️
📓 code_03_XX A basic Agent-Router application
    ⬇️
📝 03.03 Setting up Indexes
```

---

## الخطوة الأولى: تثبيت المتطلبات (Dependencies)

### الـ Packages المطلوبة:

#### 1. llama-index
- ده **الـ Framework** الرئيسي اللي هنشتغل بيه

#### 2. Azure OpenAI
- هنستخدمه كـ **LLM** (نموذج اللغة الكبير)

### تشغيل التثبيت:
```python
# تثبيت الحزم
- اختار Python Environment
- اختار Python 3.10
- شغل الكود
```

---

## الخطوة الثانية: إعداد الاتصال بالـ LLM

### المكونات المطلوبة:

#### 1. Asyncio
```
🔌 Asyncio
    ⬇️
مطلوب للعمل مع llama-index
```

#### 2. إعدادات Azure OpenAI

**المعلومات المطلوبة:**
- `api_key` - **مفتاح الـ API**
- `azure_endpoint` - **عنوان الخدمة**
- `api_version` - **إصدار الـ API**

> ⚠️ **تحذير مهم:**
> لازم **تستبدل** الـ api_key والـ endpoint **بالقيم بتاعتك** من حسابك الخاص، وإلا الكود **مش هيشتغل**!

#### 3. استخدام LLM مختلف (اختياري)
```
إذا كنت عايز تستخدم LLM تاني:
    ⬇️
غير إعدادات الـ LLM والـ Embedding
    ⬇️
راجع توثيق llama-index
```

---

## الخطوة الثالثة: إنشاء Objects الـ LLM

### 1. إنشاء AzureOpenAI LLM Object

```python
# هنستخدم موديل GPT-3.5 Turbo
LLM Object ⬅️ GPT-3.5 Turbo
```

**الموديل المستخدم:**
- **GPT-3.5 Turbo** - سريع وفعال للتمرين ده

### 2. إنشاء AzureOpenAIEmbedding Object

```python
# ده المسؤول عن تحويل النصوص لأرقام
Embedding Object ⬅️ Azure OpenAI
```

### تشغيل الكود:
```
▶️ Run الكود
    ⬇️
✅ الـ Notebook Status بيوضح إنه خلص
```

---

## الخطوة الرابعة: إنشاء Indexes للمستندات

### موقع المستندات:
```
📁 نفس مجلد التمرين
    ├── 📄 AeroFlow.pdf
    └── 📄 EcoSprint.pdf
```

---

### معالجة مستند AeroFlow

#### الخطوات التفصيلية:

**1. تحميل الملف**
```python
simple_directory_reader() ⬅️ قراءة الملف
```
- نستخدم **SimpleDirectoryReader** عشان نحمل الملف

**2. تقسيم المستند**
```python
Document ➡️ Chunks (قطع صغيرة)
```
- نقسم المستند لـ **قطع صغيرة** (Chunks)

**3. إنشاء Vector Store**
```python
Chunks ➡️ Embeddings ➡️ Vector Store (في الذاكرة)
```
- نحول الـ Chunks لـ **Embeddings**
- نحفظهم في **Vector Store** في الذاكرة

**4. إنشاء Query Engine**
```python
as_query_engine() ⬅️ إنشاء محرك البحث
```
- نستخدم method اسمه **as_query_engine**
- يبقى عندنا محرك بحث جاهز

---

### معالجة مستند EcoSprint

#### نفس الخطوات بالضبط:

```
📄 EcoSprint Document
    ⬇️
📚 Load باستخدام SimpleDirectoryReader
    ⬇️
✂️ Split لـ Chunks
    ⬇️
🔢 Create Embeddings
    ⬇️
💾 Store في Vector Store
    ⬇️
🔍 Create Query Engine
    ⬇️
✅ جاهز للاستخدام
```

### تشغيل الكود:
```
▶️ Run الكود
    ⬇️
✅ خلص التنفيذ
```

---

## ملخص ما تم إنجازه

### البنية التحتية الجاهزة:

| المكون | الحالة | الوصف |
|--------|--------|-------|
| **Codespace** | ✅ جاهز | بيئة العمل |
| **Dependencies** | ✅ مثبتة | llama-index + Azure OpenAI |
| **LLM Connection** | ✅ متصل | GPT-3.5 Turbo |
| **Embedding Model** | ✅ جاهز | Azure OpenAI Embeddings |
| **AeroFlow Index** | ✅ تم إنشاؤه | محرك بحث جاهز |
| **EcoSprint Index** | ✅ تم إنشاؤه | محرك بحث جاهز |

---

## الرسم التوضيحي الكامل

```
🌐 GitHub Codespaces
         ⬇️
📦 تثبيت Packages
         ⬇️
🔌 الاتصال بـ Azure OpenAI
         ⬇️
🤖 إنشاء LLM Object (GPT-3.5)
         ⬇️
🔢 إنشاء Embedding Object
         ⬇️
    ┌────┴────┐
    ⬇️         ⬇️
📄 AeroFlow   📄 EcoSprint
    ⬇️         ⬇️
✂️ Chunks     ✂️ Chunks
    ⬇️         ⬇️
🔢 Embeddings 🔢 Embeddings
    ⬇️         ⬇️
💾 Vector Store 💾 Vector Store
    ⬇️         ⬇️
🔍 Query Engine 🔍 Query Engine
    ⬇️         ⬇️
    └────┬────┘
         ⬇️
    ✅ جاهزين للخطوة الجاية!
```

---

## نصائح مهمة

### ⚙️ الإعدادات:
1. **استخدم Python 3.10** - ده الإصدار الموصى بيه
2. **ما تنساش api_key** - حط المفتاح بتاعك
3. **اتأكد من الـ endpoint** - لازم يكون صحيح

### 🐛 لو حصلت مشكلة:
- **تحقق من الـ API credentials**
- **راجع توثيق llama-index** لو عايز تستخدم LLM تاني
- **تأكد إن الـ Packages اتثبتت صح**

---

## الخطوة الجاية

في الفيديو الجاي هنعمل:
- **إعداد الـ Router** نفسه
- **ربط الـ Query Engines** بالـ Router
- **تجربة النظام** بأسئلة حقيقية

### استعد! 🚀 الجزء المثير جاي!

</div>